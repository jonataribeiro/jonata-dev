/// <reference types="vite/client" />

/**
 * Diretiva de referência do TypeScript.
 * Informa ao compilador para incluir os tipos globais do ambiente Vite, 
 * permitindo o uso de variáveis como `import.meta.env` ou importação 
 * de arquivos estáticos (como imagens e CSS) sem erros de tipo.
 */